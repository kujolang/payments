{
  "type": "object",
  "properties": {
    "schema": {"const": "kujo.link-approval-maintenance/v1"},
    "database": {"type": "string", "minLength": 1, "maxLength": 4096},
    "action": {"enum": ["migrate", "retire"]},
    "quiesced": {"type": "boolean"},
    "batch_limit": {"type": "integer", "minimum": 1, "maximum": 1000}
  },
  "required": ["schema", "database", "action"],
  "additionalProperties": false
}
